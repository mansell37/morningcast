# --- core ---
anthropic>=0.40.0
openai>=1.40.0          # used for xAI Grok (OpenAI-compatible endpoint)
python-dotenv>=1.0.0

# --- web + scheduling ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9 # form posts in the web UI
schedule>=1.2.0

# --- audio ---
# Kokoro: free CPU-friendly TTS; the default audio backend.
# imageio-ffmpeg: bundled ffmpeg binary so deploys don't depend on apt/nix/
#   winget. ~30 MB; provides ffmpeg via imageio_ffmpeg.get_ffmpeg_exe().
# soundfile: WAV I/O.
# Together these pull in torch + transformers (~2 GB), making images big.
soundfile>=0.12.1
imageio-ffmpeg>=0.5.0
kokoro>=0.7.0

# --- audio (optional backends, not required) ---
# elevenlabs>=1.0.0     # uncomment to use the paid ElevenLabs backend
#
# Dia2 (build-on-PC backend): GPU-only, so it is NOT installed on the server.
# On the PC that runs `python -m morningcast.worker`, install it alongside a
# CUDA-enabled torch:
#   pip install git+https://github.com/nari-labs/dia.git
# (the repo's distribution is named `nari-tts`; the import module is `dia`.)
# The worker uses only the standard library for HTTP, so nothing else is needed.

# --- dev ---
pytest>=8.0.0
